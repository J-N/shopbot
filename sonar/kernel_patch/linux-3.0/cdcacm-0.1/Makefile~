obj-m := cdcacm.o
KVERSION := $(shell uname -r)
#KERNELVERSION="`uname -r`"
PWD := $(shell pwd)
#export CFLAGS = "-DPATCH_DISABLED"

all:
	$(MAKE) -C /lib/modules/$(KVERSION)/build M=$(PWDf) modules

clean:
	$(MAKE) -C /lib/modules/$(KVERSION)/build M=$(PWD) clean
